# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/

# benchmark outputs (regenerable)
bench/results/
*.log

# secrets — never commit (the bench client reads the key from here / env)
.ultracode-bench/
*deepseek*.env
.env

# fetched benchmark data (use the fetch_*.py scripts)
bench/data/*.json
bench/data/*.jsonl

# session scratch
bench/_review2_corpus.txt
__pycache__/
*.pyc
