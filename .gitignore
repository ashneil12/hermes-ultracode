# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/

# benchmark outputs (regenerable)
bench/results/
*.log

# secrets — never commit (the bench client reads the key from here / env)
.ultracode-bench/
*deepseek*.env
.env
